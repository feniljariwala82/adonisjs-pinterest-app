<div class="row mt-4" data-masonry='{"percentPosition": true }'>

  @if(posts.length)
  {{--  list post view component starts  --}}
  <div class="col-6 col-sm-6 col-md-4 col-lg-3">
    @each(post in posts)
    @!component('components/post/list_item', {
    posts
    })
    @endeach
  </div>
  {{--  list post view component ends  --}}
  @else
  @!component('components/post/create_post_msg')
  @endif

</div>
